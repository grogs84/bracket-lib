import {
  require_jsx_runtime
} from "./chunk-WLVB5OIP.js";
import "./chunk-CANBAPAS.js";
import {
  __toESM
} from "./chunk-5WRI5ZAA.js";

// node_modules/@mgi/bracket-react/dist/index.js
var import_jsx_runtime = __toESM(require_jsx_runtime());
function BracketSVG(props) {
  const {
    layout,
    width,
    height,
    boxWidth = 120,
    boxHeight = 40,
    padding = 20,
    stroke = "#888",
    fill = "#fff",
    textColor = "#222"
  } = props;
  const maxX = Math.max(0, ...layout.nodes.map((n) => n.x));
  const maxY = Math.max(0, ...layout.nodes.map((n) => n.y));
  const w = width ?? maxX + boxWidth + padding * 2;
  const h = height ?? maxY + boxHeight + padding * 2;
  const nodeMap = new Map(layout.nodes.map((n) => [n.id, n]));
  const boxX = (x) => padding + x;
  const boxY = (y) => padding + y;
  const midY = (y) => boxY(y) + boxHeight / 2;
  const rightX = (x) => boxX(x) + boxWidth;
  const leftX = (x) => boxX(x);
  return (0, import_jsx_runtime.jsxs)("svg", { width: w, height: h, viewBox: `0 0 ${w} ${h}`, role: "img", children: [
    (0, import_jsx_runtime.jsx)("g", { stroke, fill: "none", strokeWidth: 2, children: layout.edges.map((e) => {
      const from = nodeMap.get(e.from);
      const to = nodeMap.get(e.to);
      const x1 = rightX(from.x);
      const y1 = midY(from.y);
      const x4 = leftX(to.x);
      const y4 = midY(to.y);
      const mid = (x1 + x4) / 2;
      const d = `M ${x1},${y1} H ${mid} V ${y4} H ${x4}`;
      return (0, import_jsx_runtime.jsx)("path", { d }, `${e.from}->${e.to}`);
    }) }),
    (0, import_jsx_runtime.jsx)("g", { children: layout.nodes.map((n) => (0, import_jsx_runtime.jsxs)("g", { transform: `translate(${boxX(n.x)}, ${boxY(n.y)})`, children: [
      (0, import_jsx_runtime.jsx)("rect", { width: boxWidth, height: boxHeight, rx: 6, ry: 6, fill, stroke }),
      (0, import_jsx_runtime.jsx)(
        "text",
        {
          x: boxWidth / 2,
          y: boxHeight / 2,
          dominantBaseline: "middle",
          textAnchor: "middle",
          fill: textColor,
          fontFamily: "system-ui, sans-serif",
          fontSize: 12,
          children: n.id
        }
      )
    ] }, n.id)) })
  ] });
}
export {
  BracketSVG
};
//# sourceMappingURL=@mgi_bracket-react.js.map
