{
  "name": "bracket-lib",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -ws test",
    "lint": "echo \"lint placeholder\"",
    "dev": "npm --prefix examples/react-vite run dev",
    "preview": "npm --prefix examples/react-vite run preview"
  }
}
